# claude-for-alexa

This repo holds two projects:

- `voiceagent/`: the Python voice assistant (brain plus phone and Mac satellites). This is the active project. Its full context and handoff notes are imported below.
- Repo root (`server.js`, `skill-package/`, `ask-resources.json`): the earlier Alexa to OpenClaw bridge in Node. Kept for reference.

@voiceagent/CLAUDE.md
